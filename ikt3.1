import matplotlib.pyplot as plt
import numpy as np
from numpy import exp, diff

fig, ax = plt.subplots()

x = np.linspace(0, 100)
f = exp(x ** 3 - 3) / (x ** 5 - 2 * x ** 4 + 3 * x ** 2 - 5)

plt.ylabel('Y axis')
plt.xlabel('X axis')

ax.plot(x, y)
plt.show()
